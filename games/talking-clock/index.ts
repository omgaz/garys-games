/**
 *
 * @param time - Input type in format HH:MM e.g. 12:55.
 * @returns Input time as a ”spoken’ string e.g. It’s twelve fifty five pm.
 */
const talkingClock = (time: string): string => {
  /**
   * Your code here.
   */

  return `It’s ${time}`;
}

export default talkingClock;
